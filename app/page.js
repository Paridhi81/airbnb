'use client'

import { useEffect, useMemo, useState } from 'react'
import {
  ArrowLeft, ArrowRight, Bath, BedDouble, Bell, CalendarDays, ChevronDown, ChevronLeft,
  ChevronRight, CircleUserRound, Compass, CreditCard, Filter, Globe2, Heart, Home, HousePlus, KeyRound,
  Map, Menu, MessageCircle, Minus, Mountain, Plus, Search, ShieldCheck, SlidersHorizontal,
  Sparkles, Star, TentTree, Trash2, Users, Wifi, X
} from 'lucide-react'

const curatedImages = [
  'https://images.unsplash.com/photo-1583847268964-b28dc8f51f92?auto=format&fit=crop&w=1200&q=85',
  'https://images.unsplash.com/photo-1613977257365-aaae5a9817ff?auto=format&fit=crop&w=1200&q=85',
  'https://images.unsplash.com/photo-1641232458416-feace752b346?auto=format&fit=crop&w=1200&q=85',
  'https://images.unsplash.com/photo-1613490493576-7fde63acd811?auto=format&fit=crop&w=1200&q=85',
  'https://images.unsplash.com/photo-1505693416388-ac5ce068fe85?auto=format&fit=crop&w=1200&q=85',
  'https://images.unsplash.com/photo-1449158743715-0a90ebb6d2d8?auto=format&fit=crop&w=1200&q=85',
  'https://images.unsplash.com/photo-1600607687939-ce8a6c25118c?auto=format&fit=crop&w=1200&q=85',
  'https://images.unsplash.com/photo-1510798831971-661eb04b3739?auto=format&fit=crop&w=1200&q=85'
]

const fallbackListings = [
  { id: 'stay-01', title: 'Sunlit apartment with skyline views', location: 'Sector 63, Noida', price: 3700, rating: 4.92, reviews: 86, type: 'Apartment', guests: 4, bedrooms: 2, beds: 2, baths: 2, host: 'Riya', hostInitials: 'RK', hostColor: '#f9c5b7', badge: 'Guest favourite', description: 'Wake up to a wide city view in this calm, design-led home with plenty of light, a chef-ready kitchen, and a dedicated work corner.', amenities: ['Wifi', 'Kitchen', 'Workspace', 'Air conditioning'], images: [curatedImages[0], curatedImages[2], curatedImages[1]] },
  { id: 'stay-02', title: 'Warm villa tucked into a quiet garden', location: 'Chattarpur, New Delhi', price: 5800, rating: 4.88, reviews: 121, type: 'Villa', guests: 6, bedrooms: 3, beds: 4, baths: 3, host: 'Arjun', hostInitials: 'AS', hostColor: '#bfe2d0', badge: 'Guest favourite', description: 'A leafy hideaway for slow mornings, long lunches, and evenings around the fire pit. The garden is all yours.', amenities: ['Wifi', 'Pool', 'Free parking', 'Kitchen'], images: [curatedImages[1], curatedImages[3], curatedImages[6]] },
  { id: 'stay-03', title: 'Peaceful home near Lodhi Garden', location: 'Lodhi Colony, New Delhi', price: 4400, rating: 4.97, reviews: 64, type: 'Home', guests: 3, bedrooms: 1, beds: 2, baths: 1, host: 'Meera', hostInitials: 'MP', hostColor: '#f5d29c', badge: 'Rare find', description: 'A quiet, art-filled stay in the heart of Delhi with leafy streets, independent cafés, and the city’s best morning walks nearby.', amenities: ['Wifi', 'Kitchen', 'Washer', 'Patio'], images: [curatedImages[2], curatedImages[4], curatedImages[0]] },
  { id: 'stay-04', title: 'Modern retreat with a private pool', location: 'Greater Kailash, New Delhi', price: 7200, rating: 4.86, reviews: 43, type: 'Villa', guests: 8, bedrooms: 4, beds: 5, baths: 4, host: 'Aarav', hostInitials: 'AD', hostColor: '#c6d7f4', badge: 'Guest favourite', description: 'A polished indoor-outdoor villa for celebrations and reset weekends, with a pool, terrace dining, and hotel-level comfort.', amenities: ['Wifi', 'Pool', 'Hot tub', 'Free parking'], images: [curatedImages[3], curatedImages[1], curatedImages[6]] },
  { id: 'stay-05', title: 'The little blue door in Hauz Khas', location: 'Hauz Khas, New Delhi', price: 3900, rating: 4.81, reviews: 77, type: 'Apartment', guests: 2, bedrooms: 1, beds: 1, baths: 1, host: 'Naina', hostInitials: 'NS', hostColor: '#d6c2ef', badge: 'Guest favourite', description: 'A tiny, colourful nest surrounded by art galleries, independent coffee, and the lake trail. Best for two.', amenities: ['Wifi', 'Kitchen', 'Air conditioning', 'TV'], images: [curatedImages[4], curatedImages[0], curatedImages[2]] },
  { id: 'stay-06', title: 'Terrace home overlooking the Aravallis', location: 'Aravalli Hills, Gurugram', price: 6400, rating: 4.95, reviews: 39, type: 'Home', guests: 5, bedrooms: 2, beds: 3, baths: 2, host: 'Kabir', hostInitials: 'KM', hostColor: '#f2c4c4', badge: 'Amazing views', description: 'Trade the city noise for bird song and sunset skies. This warm terrace home is made for long weekends.', amenities: ['Wifi', 'Mountain view', 'Breakfast', 'Free parking'], images: [curatedImages[5], curatedImages[0], curatedImages[4]] },
  { id: 'stay-07', title: 'A calm studio in the heart of Goa', location: 'Assagao, Goa', price: 3100, rating: 4.9, reviews: 101, type: 'Apartment', guests: 2, bedrooms: 1, beds: 1, baths: 1, host: 'Ishita', hostInitials: 'IP', hostColor: '#f6dca9', badge: 'Guest favourite', description: 'A light-filled studio with a shaded veranda, close to Goa’s best bakeries and a short scooter ride from the beach.', amenities: ['Wifi', 'Pool', 'Kitchen', 'Garden'], images: [curatedImages[6], curatedImages[2], curatedImages[1]] },
  { id: 'stay-08', title: 'Glass cabin above the cedar forest', location: 'Naukuchiatal, Uttarakhand', price: 8100, rating: 4.99, reviews: 28, type: 'Cabin', guests: 4, bedrooms: 2, beds: 2, baths: 2, host: 'Dev', hostInitials: 'DS', hostColor: '#c4dfdc', badge: 'Amazing views', description: 'Sleep beside the forest in a glass-walled cabin with a fireplace, a cedar deck, and nothing but green beyond it.', amenities: ['Wifi', 'Mountain view', 'Fireplace', 'Kitchen'], images: [curatedImages[7], curatedImages[5], curatedImages[4]] }
]

const categories = [
  { label: 'All stays', icon: Compass }, { label: 'Amazing views', icon: Mountain }, { label: 'Beach', icon: '🌊' },
  { label: 'Cabin', icon: TentTree }, { label: 'Villa', icon: Home }, { label: 'Apartment', icon: Home },
  { label: 'Trending', icon: Sparkles }, { label: 'Pool', icon: '🏊' }, { label: 'Farms', icon: '🌾' }, { label: 'OMG!', icon: '🎈' }
]

const formatMoney = (amount) => `₹${Number(amount || 0).toLocaleString('en-IN')}`
const nightsBetween = (start, end) => {
  if (!start || !end) return 0
  const days = Math.ceil((new Date(`${end}T00:00:00`) - new Date(`${start}T00:00:00`)) / 86400000)
  return days > 0 ? days : 0
}
const prettyDate = (value) => value ? new Date(`${value}T00:00:00`).toLocaleDateString('en-IN', { day: 'numeric', month: 'short' }) : 'Add date'

const IconOrEmoji = ({ icon: Icon }) => typeof Icon === 'string' ? <span className="text-[22px]">{Icon}</span> : <Icon size={22} strokeWidth={1.7} />

const App = () => {
  const [listings, setListings] = useState(fallbackListings)
  const [bookings, setBookings] = useState([])
  const [query, setQuery] = useState('')
  const [activeCategory, setActiveCategory] = useState('All stays')
  const [maxPrice, setMaxPrice] = useState(12000)
  const [guests, setGuests] = useState(0)
  const [showSearchPanel, setShowSearchPanel] = useState(false)
  const [showFilters, setShowFilters] = useState(false)
  const [showMap, setShowMap] = useState(false)
  const [showTrips, setShowTrips] = useState(false)
  const [showHost, setShowHost] = useState(false)
  const [showMenu, setShowMenu] = useState(false)
  const [showCheckout, setShowCheckout] = useState(false)
  const [comingSoon, setComingSoon] = useState('')
  const [selected, setSelected] = useState(null)
  const [liked, setLiked] = useState([])
  const [startDate, setStartDate] = useState('')
  const [endDate, setEndDate] = useState('')
  const [detailGuests, setDetailGuests] = useState(1)
  const [toast, setToast] = useState('')
  const [hostListings, setHostListings] = useState([])
  const [hostBookings, setHostBookings] = useState([])
  const [editingId, setEditingId] = useState(null)
  const [hostForm, setHostForm] = useState({ title: '', location: '', price: '', type: 'Home', guests: 2, bedrooms: 1, description: '', image: curatedImages[0] })

  const notify = (message) => { setToast(message); window.setTimeout(() => setToast(''), 3200) }

  useEffect(() => {
    fetch('/api/listings').then((response) => response.ok ? response.json() : Promise.reject()).then((data) => data?.length && setListings(data)).catch(() => notify('Showing our handpicked stays for now'))
    fetch('/api/bookings?guestId=guest-demo').then((response) => response.ok ? response.json() : []).then((data) => setBookings(data || [])).catch(() => {})
  }, [])

  const filteredListings = useMemo(() => listings.filter((listing) => {
    const text = `${listing.title} ${listing.location}`.toLowerCase()
    const matchesQuery = !query || text.includes(query.toLowerCase())
    const matchesPrice = Number(listing.price) <= maxPrice
    const matchesCategory = activeCategory === 'All stays' || activeCategory === 'Trending' || activeCategory === 'OMG!' || listing.type === activeCategory || listing.badge === activeCategory || listing.amenities.includes(activeCategory)
    return matchesQuery && matchesPrice && matchesCategory
  }), [listings, query, maxPrice, activeCategory])

  const selectedNights = nightsBetween(startDate, endDate)
  const selectedTotal = selected ? Math.round(selectedNights * selected.price * 1.14) : 0

  const openListing = (listing) => { setSelected(listing); setStartDate(''); setEndDate(''); setDetailGuests(1) }
  const toggleLike = (id) => setLiked((current) => current.includes(id) ? current.filter((item) => item !== id) : [...current, id])
  const showComingSoon = (label) => { setShowMenu(false); setComingSoon(label) }

  const reserve = async () => {
    if (!selectedNights) return notify('Choose a check-in and check-out date')
    if (detailGuests > selected.guests) return notify(`This stay hosts up to ${selected.guests} guests`)
    setShowCheckout(true)
  }

  const confirmBooking = async () => {
    try {
      const response = await fetch('/api/bookings', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ listingId: selected.id, startDate, endDate, guests: detailGuests, guestId: 'guest-demo', guestName: 'Alex Morgan' }) })
      const data = await response.json()
      if (!response.ok) return notify(data?.error || 'Those dates are unavailable')
      setBookings((current) => [data, ...current]); setShowCheckout(false); setSelected(null); setShowTrips(true); notify('Your stay is confirmed — enjoy the escape ✨')
    } catch { notify('Could not complete the reservation just yet') }
  }

  const loadHost = async () => {
    try {
      const response = await fetch('/api/host/listings?hostId=host-demo')
      const data = await response.json()
      setHostListings(data?.listings || []); setHostBookings(data?.bookings || [])
    } catch { setHostListings([]); setHostBookings([]) }
  }
  const openHost = () => { setShowHost(true); loadHost() }
  const submitHost = async (event) => {
    event.preventDefault()
    const payload = { ...hostForm, price: Number(hostForm.price), host: 'You', hostId: 'host-demo', region: hostForm.location, images: [hostForm.image || curatedImages[0]] }
    const response = await fetch(editingId ? `/api/listings/${editingId}` : '/api/listings', { method: editingId ? 'PUT' : 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) })
    if (response.ok) { notify(editingId ? 'Listing updated' : 'Listing published'); setEditingId(null); setHostForm({ title: '', location: '', price: '', type: 'Home', guests: 2, bedrooms: 1, description: '', image: curatedImages[0] }); loadHost(); fetch('/api/listings').then((r) => r.json()).then(setListings) }
    else notify('Please add a title, location, and nightly price')
  }
  const deleteHostListing = async (id) => { const response = await fetch(`/api/listings/${id}`, { method: 'DELETE' }); if (response.ok) { notify('Listing removed'); loadHost(); setListings((current) => current.filter((listing) => listing.id !== id)) } }
  const editHostListing = (listing) => { setEditingId(listing.id); setHostForm({ title: listing.title || '', location: listing.location || '', price: listing.price || '', type: listing.type || 'Home', guests: listing.guests || 2, bedrooms: listing.bedrooms || 1, description: listing.description || '', image: listing.images?.[0] || curatedImages[0] }) }

  return (
    <main className="min-h-screen bg-white text-[#222222]">
      <header className="sticky top-0 z-30 border-b border-[#ebebeb] bg-white/95 backdrop-blur">
        <div className="mx-auto flex h-[76px] max-w-[1440px] items-center justify-between px-5 lg:px-10">
          <button onClick={() => { setQuery(''); setActiveCategory('All stays'); setShowTrips(false); setShowHost(false) }} className="flex items-center gap-2.5" aria-label="Roamly home">
            <span className="relative flex h-9 w-9 items-center justify-center text-[#ff385c]"><span className="absolute rotate-45 rounded-[13px] border-[3px] border-[#ff385c] h-7 w-5" /><span className="absolute -rotate-45 rounded-[13px] border-[3px] border-[#ff385c] h-7 w-5" /><span className="absolute bottom-1.5 h-2 w-2 rounded-full bg-[#ff385c]" /></span>
            <span className="text-[26px] font-bold tracking-[-1.5px] text-[#ff385c]">roamly</span>
          </button>
          <nav className="hidden items-center gap-8 md:flex">
            {[['All', Compass], ['Homes', Home], ['Experiences', Sparkles], ['Services', Bell]].map(([label, Icon], index) => <button key={label} onClick={() => index === 0 ? setActiveCategory('All stays') : showComingSoon(label)} className={`flex items-center gap-2 border-b-[3px] py-6 text-[15px] font-semibold ${index === 0 ? 'border-[#222222] text-[#222222]' : 'border-transparent text-[#717171]'}`}><Icon size={22} strokeWidth={1.6} />{label}</button>)}
          </nav>
          <div className="flex items-center gap-2.5 text-sm font-semibold">
            <button onClick={openHost} className="hidden rounded-full px-4 py-3 transition hover:bg-[#f7f7f7] lg:block">Roamly your home</button>
            <button onClick={() => showComingSoon('Language and currency')} className="hidden rounded-full p-3 hover:bg-[#f7f7f7] sm:block"><Globe2 size={19} /></button>
            <button onClick={() => setShowMenu(true)} className="flex items-center gap-2 rounded-full border border-[#dddddd] p-2.5 pl-3 shadow-sm transition hover:shadow-md"><Menu size={18} /><CircleUserRound size={25} strokeWidth={1.5} /></button>
          </div>
        </div>
        <div className="mx-auto max-w-[880px] px-5 pb-5 md:hidden"><SearchBar compact query={query} setQuery={setQuery} setShowSearchPanel={setShowSearchPanel} /></div>
        <div className="mx-auto hidden max-w-[850px] px-5 pb-5 md:block"><SearchBar query={query} setQuery={setQuery} setShowSearchPanel={setShowSearchPanel} /></div>
      </header>

      <div className="mx-auto max-w-[1440px] px-5 lg:px-10">
        <section className="flex items-center gap-7 overflow-x-auto border-b border-[#eeeeee] py-5 scrollbar-none">
          {categories.map(({ label, icon: Icon }) => <button key={label} onClick={() => setActiveCategory(label === 'All stays' ? 'All stays' : label)} className={`group flex min-w-fit flex-col items-center gap-2 border-b-2 pb-2 text-xs font-medium transition ${activeCategory === label ? 'border-[#222222] text-[#222222]' : 'border-transparent text-[#717171] hover:text-[#222222]'}`}><IconOrEmoji icon={Icon} /><span>{label}</span></button>)}
        </section>
        <section className="flex items-center justify-between py-5">
          <div><h1 className="text-[25px] font-semibold tracking-[-.7px]">Stays that feel like a getaway</h1><p className="mt-1 text-sm text-[#717171]">Curated homes for your next long weekend</p></div>
          <div className="flex items-center gap-2"><button onClick={() => setShowFilters(true)} className="flex items-center gap-2 rounded-full border border-[#dddddd] px-4 py-3 text-sm font-semibold hover:border-[#222222]"><SlidersHorizontal size={16} /> Filters</button><button onClick={() => setShowMap((value) => !value)} className={`hidden items-center gap-2 rounded-full border px-4 py-3 text-sm font-semibold sm:flex ${showMap ? 'border-[#222222] bg-[#f7f7f7]' : 'border-[#dddddd]'}`}><Map size={16} /> {showMap ? 'Hide map' : 'Show map'}</button></div>
        </section>

        <div className={`${showMap ? 'grid gap-6 lg:grid-cols-[1fr_360px]' : ''}`}>
          <section className="grid grid-cols-1 gap-x-5 gap-y-9 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4">
            {filteredListings.map((listing) => <ListingCard key={listing.id} listing={listing} liked={liked.includes(listing.id)} onLike={() => toggleLike(listing.id)} onOpen={() => openListing(listing)} />)}
            {!filteredListings.length && <div className="col-span-full rounded-2xl border border-dashed border-[#dddddd] py-20 text-center"><Search className="mx-auto text-[#717171]" /><h2 className="mt-4 text-xl font-semibold">No stays found</h2><p className="mt-2 text-sm text-[#717171]">Try a different location or loosen your filters.</p><button onClick={() => { setQuery(''); setMaxPrice(12000); setActiveCategory('All stays') }} className="mt-5 rounded-lg bg-[#222222] px-5 py-3 text-sm font-semibold text-white">Clear search</button></div>}
          </section>
          {showMap && <aside className="hidden min-h-[650px] overflow-hidden rounded-2xl bg-[#dce9df] lg:sticky lg:top-36 lg:block"><div className="relative h-full bg-[radial-gradient(circle_at_35%_30%,#f5e6c8_0_15%,transparent_16%),linear-gradient(130deg,#dce9df,#c4d9d0)]"><div className="absolute inset-0 opacity-30 [background-image:linear-gradient(35deg,transparent_48%,#8eb3a2_49%,transparent_51%),linear-gradient(120deg,transparent_48%,#8eb3a2_49%,transparent_51%)] [background-size:90px_90px]" />{filteredListings.slice(0, 6).map((listing, index) => <button key={listing.id} onClick={() => openListing(listing)} className="absolute rounded-full border-2 border-white bg-[#222222] px-3 py-2 text-xs font-bold text-white shadow-xl transition hover:scale-110" style={{ left: `${18 + (index * 13) % 68}%`, top: `${18 + (index * 17) % 66}%` }}>{formatMoney(listing.price)}</button>)}<div className="absolute bottom-5 left-5 rounded-xl bg-white/90 px-4 py-3 text-sm font-semibold shadow-lg">Explore the map <span className="ml-2 text-[#717171]">{filteredListings.length} stays</span></div></div></aside>}
        </div>
      </div>

      {showSearchPanel && <SearchPanel query={query} setQuery={setQuery} guests={guests} setGuests={setGuests} onClose={() => setShowSearchPanel(false)} onSearch={() => setShowSearchPanel(false)} />}
      {showFilters && <FilterPanel maxPrice={maxPrice} setMaxPrice={setMaxPrice} onClose={() => setShowFilters(false)} />}
      {selected && <ListingDetail listing={selected} startDate={startDate} setStartDate={setStartDate} endDate={endDate} setEndDate={setEndDate} guests={detailGuests} setGuests={setDetailGuests} nights={selectedNights} total={selectedTotal} liked={liked.includes(selected.id)} onLike={() => toggleLike(selected.id)} onClose={() => setSelected(null)} onReserve={reserve} onMessage={() => showComingSoon('Messaging with hosts')} />}
      {showTrips && <TripsModal bookings={bookings} onClose={() => setShowTrips(false)} />}
      {showHost && <HostModal listings={hostListings} bookings={hostBookings} form={hostForm} setForm={setHostForm} editingId={editingId} onClose={() => { setShowHost(false); setEditingId(null) }} onSubmit={submitHost} onEdit={editHostListing} onDelete={deleteHostListing} onComingSoon={showComingSoon} />}
      {showCheckout && selected && <CheckoutModal listing={selected} nights={selectedNights} total={selectedTotal} onClose={() => setShowCheckout(false)} onConfirm={confirmBooking} />}
      {showMenu && <MenuModal onClose={() => setShowMenu(false)} onTrips={() => { setShowMenu(false); setShowTrips(true) }} onHost={() => { setShowMenu(false); openHost() }} onComingSoon={showComingSoon} />}
      {comingSoon && <ComingSoonModal label={comingSoon} onClose={() => setComingSoon('')} />}
      {toast && <div className="fixed bottom-6 left-1/2 z-[70] -translate-x-1/2 rounded-full bg-[#222222] px-5 py-3 text-sm font-semibold text-white shadow-2xl">{toast}</div>}
    </main>
  )
}

const SearchBar = ({ query, setQuery, setShowSearchPanel, compact }) => <div className={`flex items-center rounded-full border border-[#dddddd] bg-white shadow-[0_5px_20px_rgba(0,0,0,.10)] transition hover:shadow-[0_7px_25px_rgba(0,0,0,.14)] ${compact ? 'h-14' : 'h-[66px]'}`}>
  <button onClick={() => setShowSearchPanel(true)} className="min-w-0 flex-1 px-6 text-left"><span className="block text-[12px] font-bold">Where</span><span className="block truncate text-sm text-[#717171]">{query || 'Search destinations'}</span></button><span className="h-8 w-px bg-[#dddddd]" /><button onClick={() => setShowSearchPanel(true)} className="hidden min-w-[190px] flex-1 px-6 text-left sm:block"><span className="block text-[12px] font-bold">When</span><span className="block text-sm text-[#717171]">Add dates</span></button><span className="hidden h-8 w-px bg-[#dddddd] sm:block" /><button onClick={() => setShowSearchPanel(true)} className="hidden min-w-[150px] flex-1 px-6 text-left md:block"><span className="block text-[12px] font-bold">Who</span><span className="block text-sm text-[#717171]">Add guests</span></button><button onClick={() => setShowSearchPanel(true)} className="mr-2 flex h-12 w-12 shrink-0 items-center justify-center rounded-full bg-[#ff385c] text-white transition hover:bg-[#e61e4d]"><Search size={21} strokeWidth={2.5} /></button>
</div>

const ListingCard = ({ listing, liked, onLike, onOpen }) => <article className="group cursor-pointer" onClick={onOpen}><div className="relative aspect-[1.02/1] overflow-hidden rounded-2xl bg-[#f1f1f1]"><img src={listing.images?.[0]} alt={listing.title} className="h-full w-full object-cover transition duration-500 group-hover:scale-[1.04]" /><div className="absolute left-3 top-3 rounded-full bg-white/95 px-3 py-1.5 text-xs font-bold shadow-sm">{listing.badge || 'New stay'}</div><button onClick={(event) => { event.stopPropagation(); onLike() }} className="absolute right-3 top-3 rounded-full p-1 text-white drop-shadow-[0_1px_2px_rgba(0,0,0,.7)] transition hover:scale-110" aria-label="Add to wishlist"><Heart size={25} fill={liked ? '#ff385c' : 'rgba(0,0,0,.25)'} color={liked ? '#ff385c' : 'white'} strokeWidth={1.8} /></button><div className="absolute inset-x-0 bottom-0 h-20 bg-gradient-to-t from-black/20 to-transparent opacity-0 transition group-hover:opacity-100" /></div><div className="px-1 pt-3"><div className="flex items-start justify-between gap-2"><h2 className="line-clamp-1 text-[15px] font-semibold">{listing.title}</h2><span className="flex shrink-0 items-center gap-1 text-sm"><Star size={13} fill="#222" /> {Number(listing.rating || 0).toFixed(2)}</span></div><p className="mt-1 text-sm text-[#717171]">{listing.location}</p><p className="mt-2 text-sm"><strong>{formatMoney(listing.price)}</strong> night <span className="text-[#717171]">· {listing.reviews || 0} reviews</span></p></div></article>

const SearchPanel = ({ query, setQuery, guests, setGuests, onClose, onSearch }) => <Overlay onClose={onClose} title="Search stays"><div className="space-y-4"><label className="block text-sm font-semibold">Where<input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search by city, neighbourhood, or landmark" className="mt-2 w-full rounded-xl border border-[#b0b0b0] px-4 py-3 text-base outline-none focus:border-[#222222]" autoFocus /></label><div className="grid grid-cols-2 gap-3"><label className="text-sm font-semibold">Check in<input type="date" className="mt-2 w-full rounded-xl border border-[#b0b0b0] px-3 py-3 font-normal" /></label><label className="text-sm font-semibold">Check out<input type="date" className="mt-2 w-full rounded-xl border border-[#b0b0b0] px-3 py-3 font-normal" /></label></div><label className="block text-sm font-semibold">Guests<div className="mt-2 flex items-center justify-between rounded-xl border border-[#b0b0b0] px-4 py-3"><span className="flex items-center gap-2 font-normal"><Users size={18} /> {guests || 'Add guests'}</span><span className="flex items-center gap-3"><button onClick={() => setGuests(Math.max(0, guests - 1))} className="rounded-full border p-1"><Minus size={15} /></button><button onClick={() => setGuests(guests + 1)} className="rounded-full border p-1"><Plus size={15} /></button></span></div></label><button onClick={onSearch} className="mt-2 flex w-full items-center justify-center gap-2 rounded-xl bg-[#ff385c] py-3.5 font-bold text-white hover:bg-[#e61e4d]"><Search size={18} /> Search stays</button></div></Overlay>

const FilterPanel = ({ maxPrice, setMaxPrice, onClose }) => <Overlay onClose={onClose} title="Filters"><div className="space-y-7"><div><p className="font-semibold">Price range</p><p className="mt-1 text-sm text-[#717171]">Nightly prices before taxes</p><input type="range" min="1500" max="12000" step="500" value={maxPrice} onChange={(event) => setMaxPrice(event.target.value)} className="mt-6 w-full accent-[#ff385c]" /><div className="mt-3 flex justify-between text-sm"><span>₹1,500</span><strong>Up to {formatMoney(maxPrice)}</strong></div></div><div className="border-t border-[#eeeeee] pt-6"><p className="font-semibold">Property type</p><div className="mt-4 grid grid-cols-2 gap-3">{['Home', 'Apartment', 'Villa', 'Cabin'].map((type) => <button key={type} className="rounded-xl border border-[#dddddd] px-3 py-4 text-left text-sm hover:border-[#222222]"><Home size={20} className="mb-3" />{type}</button>)}</div></div><div className="border-t border-[#eeeeee] pt-6"><p className="font-semibold">Amenities</p><div className="mt-3 flex flex-wrap gap-2">{['Wifi', 'Pool', 'Kitchen', 'Free parking', 'Mountain view'].map((amenity) => <span key={amenity} className="rounded-full border border-[#dddddd] px-3 py-2 text-sm">{amenity}</span>)}</div></div><button onClick={onClose} className="w-full rounded-xl bg-[#222222] py-3.5 font-bold text-white">Show stays</button></div></Overlay>

const ListingDetail = ({ listing, startDate, setStartDate, endDate, setEndDate, guests, setGuests, nights, total, liked, onLike, onClose, onReserve }) => <div className="fixed inset-0 z-50 overflow-y-auto bg-white"><div className="mx-auto max-w-[1240px] px-5 py-5 lg:px-10"><div className="flex items-center justify-between"><button onClick={onClose} className="flex items-center gap-2 rounded-full px-2 py-2 text-sm font-semibold hover:bg-[#f7f7f7]"><ChevronLeft size={18} /> Back to stays</button><div className="flex gap-2"><button onClick={onLike} className="flex items-center gap-2 rounded-full px-3 py-2 text-sm font-semibold underline hover:bg-[#f7f7f7]"><Heart size={18} fill={liked ? '#ff385c' : 'none'} color={liked ? '#ff385c' : '#222'} /> {liked ? 'Saved' : 'Save'}</button><button className="rounded-full px-3 py-2 text-sm font-semibold underline hover:bg-[#f7f7f7]"><ArrowRight size={18} className="inline rotate-[-45deg]" /> Share</button></div></div><div className="mt-5 grid h-[360px] grid-cols-4 grid-rows-2 gap-2 overflow-hidden rounded-2xl lg:h-[460px]"><img src={listing.images?.[0]} alt="Main view" className="col-span-2 row-span-2 h-full w-full object-cover" />{listing.images?.slice(1, 3).map((image, index) => <img key={image} src={image} alt={`Gallery ${index + 2}`} className="h-full w-full object-cover" />)}<div className="relative overflow-hidden"><img src={listing.images?.[0]} alt="Gallery detail" className="h-full w-full object-cover brightness-90" /><span className="absolute bottom-4 right-4 rounded-lg bg-white px-3 py-2 text-xs font-bold">View all photos</span></div></div><div className="grid gap-10 py-8 lg:grid-cols-[1fr_390px]"><div><div className="flex items-start justify-between gap-4"><div><p className="text-sm font-semibold text-[#717171]">{listing.type} in {listing.location}</p><h1 className="mt-2 text-2xl font-semibold tracking-[-.6px] lg:text-[30px]">{listing.title}</h1><p className="mt-3 text-sm"><strong>{listing.guests} guests</strong> · <strong>{listing.bedrooms} bedrooms</strong> · <strong>{listing.beds} beds</strong> · <strong>{listing.baths} baths</strong></p></div><div className="flex h-12 w-12 shrink-0 items-center justify-center rounded-full text-sm font-bold" style={{ backgroundColor: listing.hostColor || '#f5d29c' }}>{listing.hostInitials || 'YO'}</div></div><div className="my-8 border-y border-[#eeeeee] py-6"><div className="flex items-center gap-4"><div className="flex h-11 w-11 items-center justify-center rounded-full bg-[#f7f7f7]"><Sparkles size={21} /></div><div><p className="font-semibold">Hosted by {listing.host || 'your host'}</p><p className="mt-1 text-sm text-[#717171]">A top-rated host · Welcomes you to Roamly</p></div></div></div><p className="leading-7 text-[#484848]">{listing.description}</p><div className="mt-8"><h2 className="text-xl font-semibold">What this place offers</h2><div className="mt-5 grid grid-cols-2 gap-y-5 sm:grid-cols-3">{(listing.amenities || []).map((amenity) => <div key={amenity} className="flex items-center gap-3 text-sm"><Wifi size={19} strokeWidth={1.5} />{amenity}</div>)}</div></div><div className="mt-10 border-t border-[#eeeeee] pt-8"><h2 className="text-xl font-semibold"><Star size={19} className="mr-1 inline" fill="#222" /> {Number(listing.rating || 0).toFixed(2)} · {listing.reviews || 0} reviews</h2><div className="mt-6 grid gap-5 sm:grid-cols-2">{['The home was exactly as pictured and beautifully quiet.', 'Wonderful host, thoughtful details, and a perfect location.'].map((review, index) => <div key={review} className="rounded-xl bg-[#f7f7f7] p-4 text-sm leading-6"><div className="mb-2 flex items-center gap-2 font-semibold"><span className="flex h-8 w-8 items-center justify-center rounded-full bg-[#e5c4ba] text-xs">{index ? 'SM' : 'AS'}</span>{index ? 'Sana M.' : 'Aditi S.'}</div>{review}</div>)}</div></div></div><aside className="h-fit rounded-2xl border border-[#dddddd] p-6 shadow-[0_5px_20px_rgba(0,0,0,.10)] lg:sticky lg:top-5"><p className="text-xl"><strong>{formatMoney(listing.price)}</strong> <span className="text-sm font-normal">night</span></p><div className="mt-5 grid grid-cols-2 overflow-hidden rounded-xl border border-[#999999]"><label className="border-r border-[#999999] p-3 text-[10px] font-bold uppercase">Check in<input type="date" value={startDate} onChange={(event) => setStartDate(event.target.value)} className="mt-1 block w-full text-sm font-normal outline-none" /></label><label className="p-3 text-[10px] font-bold uppercase">Check out<input type="date" value={endDate} onChange={(event) => setEndDate(event.target.value)} className="mt-1 block w-full text-sm font-normal outline-none" /></label></div><div className="mt-3 flex items-center justify-between rounded-xl border border-[#999999] p-3"><span className="text-[10px] font-bold uppercase">Guests</span><span className="flex items-center gap-3"><button onClick={() => setGuests(Math.max(1, guests - 1))} className="rounded-full border p-1"><Minus size={14} /></button><span className="text-sm">{guests}</span><button onClick={() => setGuests(Math.min(listing.guests, guests + 1))} className="rounded-full border p-1"><Plus size={14} /></button></span></div><button onClick={onReserve} className="mt-5 w-full rounded-xl bg-[#ff385c] py-3.5 font-bold text-white hover:bg-[#e61e4d]">Reserve</button>{nights > 0 ? <div className="mt-5 space-y-3 border-t border-[#eeeeee] pt-5 text-sm"><div className="flex justify-between"><span className="underline">{formatMoney(listing.price)} × {nights} nights</span><span>{formatMoney(nights * listing.price)}</span></div><div className="flex justify-between"><span className="underline">Roamly service fee</span><span>{formatMoney(Math.round(nights * listing.price * .14))}</span></div><div className="flex justify-between border-t border-[#eeeeee] pt-4 text-base font-bold"><span>Total before taxes</span><span>{formatMoney(total)}</span></div></div> : <p className="mt-4 text-center text-xs text-[#717171]">You won’t be charged yet</p>}</aside></div></div></div>

const TripsModal = ({ bookings, onClose }) => <Overlay onClose={onClose} title="My trips"><div className="mb-5 rounded-2xl bg-[#fdf1ee] p-5"><p className="text-sm font-semibold">Your next adventure is one click away</p><p className="mt-1 text-sm text-[#717171]">Confirmed stays and memorable weekends, all in one place.</p></div>{bookings.length ? <div className="space-y-4">{bookings.map((booking) => <div key={booking.id} className="flex gap-4 rounded-2xl border border-[#eeeeee] p-3"><img src={booking.listingImage} alt={booking.listingTitle} className="h-24 w-28 rounded-xl object-cover" /><div className="min-w-0"><p className="text-xs font-semibold uppercase tracking-wide text-[#717171]">{booking.status}</p><h3 className="mt-1 truncate font-semibold">{booking.listingTitle}</h3><p className="mt-1 text-sm text-[#717171]">{booking.location}</p><p className="mt-2 text-sm font-medium">{prettyDate(booking.startDate)} – {prettyDate(booking.endDate)} · {booking.guests} guests</p></div></div>)}</div> : <div className="py-12 text-center"><Compass className="mx-auto text-[#717171]" size={34} /><p className="mt-4 font-semibold">No trips booked yet</p><p className="mt-1 text-sm text-[#717171]">Your next great stay is waiting.</p></div>}</Overlay>

const HostModal = ({ listings, bookings, form, setForm, editingId, onClose, onSubmit, onEdit, onDelete }) => <Overlay onClose={onClose} title="Your hosting dashboard" wide><div className="grid gap-8 lg:grid-cols-[1fr_1.15fr]"><form onSubmit={onSubmit} className="rounded-2xl bg-[#f7f7f7] p-5"><div className="flex items-center gap-2"><HousePlus size={20} /><h3 className="font-semibold">{editingId ? 'Edit your listing' : 'List your home'}</h3></div><div className="mt-5 space-y-3"><input required value={form.title} onChange={(e) => setForm({ ...form, title: e.target.value })} placeholder="Listing title" className="w-full rounded-xl border border-[#dddddd] bg-white px-4 py-3 text-sm" /><input required value={form.location} onChange={(e) => setForm({ ...form, location: e.target.value })} placeholder="Location" className="w-full rounded-xl border border-[#dddddd] bg-white px-4 py-3 text-sm" /><div className="grid grid-cols-2 gap-3"><input required type="number" value={form.price} onChange={(e) => setForm({ ...form, price: e.target.value })} placeholder="Price / night" className="w-full rounded-xl border border-[#dddddd] bg-white px-4 py-3 text-sm" /><select value={form.type} onChange={(e) => setForm({ ...form, type: e.target.value })} className="rounded-xl border border-[#dddddd] bg-white px-4 py-3 text-sm"><option>Home</option><option>Apartment</option><option>Villa</option><option>Cabin</option></select></div><div className="grid grid-cols-2 gap-3"><input type="number" min="1" value={form.guests} onChange={(e) => setForm({ ...form, guests: e.target.value })} placeholder="Guests" className="w-full rounded-xl border border-[#dddddd] bg-white px-4 py-3 text-sm" /><input type="number" min="1" value={form.bedrooms} onChange={(e) => setForm({ ...form, bedrooms: e.target.value })} placeholder="Bedrooms" className="w-full rounded-xl border border-[#dddddd] bg-white px-4 py-3 text-sm" /></div><input value={form.image} onChange={(e) => setForm({ ...form, image: e.target.value })} placeholder="Photo URL" className="w-full rounded-xl border border-[#dddddd] bg-white px-4 py-3 text-sm" /><textarea value={form.description} onChange={(e) => setForm({ ...form, description: e.target.value })} placeholder="Tell guests about your place" rows="3" className="w-full resize-none rounded-xl border border-[#dddddd] bg-white px-4 py-3 text-sm" /></div><button className="mt-4 w-full rounded-xl bg-[#222222] py-3 font-bold text-white">{editingId ? 'Save changes' : 'Publish listing'}</button>{editingId && <button type="button" onClick={() => setForm({ title: '', location: '', price: '', type: 'Home', guests: 2, bedrooms: 1, description: '', image: curatedImages[0] })} className="mt-2 w-full py-2 text-sm font-semibold underline">Cancel editing</button>}</form><div><div className="mb-4 flex items-end justify-between"><div><p className="text-sm font-semibold text-[#717171]">HOST MODE</p><h3 className="mt-1 text-xl font-semibold">Your listings</h3></div><span className="rounded-full bg-[#fdf1ee] px-3 py-1 text-sm font-semibold">{listings.length} live</span></div>{listings.length ? <div className="space-y-3">{listings.map((listing) => <div key={listing.id} className="flex items-center gap-3 rounded-xl border border-[#eeeeee] p-3"><img src={listing.images?.[0]} alt={listing.title} className="h-16 w-20 rounded-lg object-cover" /><div className="min-w-0 flex-1"><p className="truncate text-sm font-semibold">{listing.title}</p><p className="text-xs text-[#717171]">{listing.location} · {formatMoney(listing.price)} / night</p><div className="mt-2 flex gap-3"><button onClick={() => onEdit(listing)} className="text-xs font-bold underline">Edit</button><button onClick={() => onDelete(listing.id)} className="flex items-center gap-1 text-xs font-bold text-[#c13515]"><Trash2 size={12} /> Delete</button></div></div></div>)}</div> : <p className="rounded-xl border border-dashed p-8 text-center text-sm text-[#717171]">Publish your first stay to see it here.</p>}<div className="mt-7 rounded-xl border border-[#eeeeee] p-4"><h4 className="font-semibold">Upcoming guest bookings</h4>{bookings.length ? bookings.map((booking) => <p key={booking.id} className="mt-3 text-sm text-[#717171]">{booking.listingTitle} · {prettyDate(booking.startDate)} – {prettyDate(booking.endDate)}</p>) : <p className="mt-2 text-sm text-[#717171]">Your reservations will show up here.</p>}</div></div></div></Overlay>

const Overlay = ({ children, onClose, title, wide }) => <div className="fixed inset-0 z-[60] flex items-end justify-center bg-black/40 p-0 sm:items-center sm:p-5"><div className={`max-h-[92vh] w-full overflow-y-auto rounded-t-3xl bg-white p-6 shadow-2xl sm:rounded-3xl ${wide ? 'max-w-[900px]' : 'max-w-[510px]'}`}><div className="mb-6 flex items-center justify-between"><h2 className="text-xl font-semibold">{title}</h2><button onClick={onClose} className="rounded-full p-2 hover:bg-[#f7f7f7]"><X size={20} /></button></div>{children}</div></div>

const CheckoutModal = ({ listing, nights, total, onClose, onConfirm }) => <Overlay onClose={onClose} title="Confirm and pay"><div className="space-y-5"><div className="flex gap-4 rounded-2xl border border-[#eeeeee] p-3"><img src={listing.images?.[0]} alt={listing.title} className="h-20 w-24 rounded-xl object-cover" /><div><p className="font-semibold">{listing.title}</p><p className="mt-1 text-sm text-[#717171]">{prettyDate(listing.startDate)} · {nights} nights</p><p className="mt-2 text-sm font-semibold">{formatMoney(total)} total</p></div></div><div className="rounded-xl border border-[#dddddd] p-4"><div className="flex items-center gap-2 font-semibold"><CreditCard size={18} /> Payment details</div><input placeholder="Card number" className="mt-4 w-full rounded-lg border border-[#dddddd] px-3 py-3 text-sm" /><div className="mt-3 grid grid-cols-2 gap-3"><input placeholder="MM / YY" className="rounded-lg border border-[#dddddd] px-3 py-3 text-sm" /><input placeholder="CVV" className="rounded-lg border border-[#dddddd] px-3 py-3 text-sm" /></div></div><p className="flex items-start gap-2 rounded-xl bg-[#f7f7f7] p-3 text-xs leading-5 text-[#717171]"><ShieldCheck size={16} className="mt-0.5 shrink-0 text-[#222222]" /> This is a mocked checkout. No real payment is processed and your card details are not saved.</p><button onClick={onConfirm} className="w-full rounded-xl bg-[#ff385c] py-3.5 font-bold text-white hover:bg-[#e61e4d]">Confirm reservation</button></div></Overlay>

const MenuModal = ({ onClose, onTrips, onHost, onComingSoon }) => <Overlay onClose={onClose} title="Account"><div className="overflow-hidden rounded-2xl border border-[#eeeeee]"><div className="flex items-center gap-3 bg-[#f7f7f7] p-4"><div className="flex h-11 w-11 items-center justify-center rounded-full bg-[#f5c5b8] font-semibold">AM</div><div><p className="font-semibold">Alex Morgan</p><p className="text-sm text-[#717171]">Guest mode · Demo account</p></div></div><button onClick={onTrips} className="flex w-full items-center gap-3 border-b border-[#eeeeee] px-4 py-4 text-left text-sm font-semibold hover:bg-[#f7f7f7]"><CalendarDays size={18} /> My trips</button><button onClick={() => onComingSoon('Messaging with hosts')} className="flex w-full items-center gap-3 border-b border-[#eeeeee] px-4 py-4 text-left text-sm font-semibold hover:bg-[#f7f7f7]"><MessageCircle size={18} /> Messages <span className="ml-auto rounded-full bg-[#fdf1ee] px-2 py-1 text-[10px]">Coming soon</span></button><button onClick={() => onComingSoon('Identity verification')} className="flex w-full items-center gap-3 border-b border-[#eeeeee] px-4 py-4 text-left text-sm font-semibold hover:bg-[#f7f7f7]"><ShieldCheck size={18} /> Verify identity <span className="ml-auto rounded-full bg-[#fdf1ee] px-2 py-1 text-[10px]">Coming soon</span></button><button onClick={onHost} className="flex w-full items-center gap-3 border-b border-[#eeeeee] px-4 py-4 text-left text-sm font-semibold hover:bg-[#f7f7f7]"><HousePlus size={18} /> Switch to host mode</button><button onClick={() => onComingSoon('Log in and sign up')} className="flex w-full items-center gap-3 px-4 py-4 text-left text-sm font-semibold hover:bg-[#f7f7f7]"><KeyRound size={18} /> Log in or sign up</button></div></Overlay>

const ComingSoonModal = ({ label, onClose }) => <Overlay onClose={onClose} title={label}><div className="py-8 text-center"><div className="mx-auto flex h-16 w-16 items-center justify-center rounded-full bg-[#fdf1ee] text-[#ff385c]"><Sparkles size={29} /></div><h3 className="mt-5 text-xl font-semibold">{label} is coming soon</h3><p className="mx-auto mt-2 max-w-sm text-sm leading-6 text-[#717171]">We’re polishing this part of the Roamly experience. For now, you can explore stays, reserve a home with our mocked checkout, or switch to host mode.</p><button onClick={onClose} className="mt-6 rounded-xl bg-[#222222] px-6 py-3 text-sm font-bold text-white">Got it</button></div></Overlay>

export default App